import {NextResponse} from 'next/server';
import {db,databaseConfigured} from '@/lib/db';
import {sameOrigin} from '@/lib/auth';
import {applicationSchema} from '@/lib/validation';
export async function POST(request:Request){if(!sameOrigin(request))return NextResponse.json({error:'Invalid request origin.'},{status:403});if(!databaseConfigured())return NextResponse.json({error:'Applications will open when the club database is connected. Your form has not been submitted.'},{status:503});try{const result=applicationSchema.safeParse(await request.json());if(!result.success)return NextResponse.json({error:result.error.issues.map(i=>`${i.path.join('.')}: ${i.message}`).join(' ')},{status:400});const{name,enrollment,email,...details}=result.data;const{data,error}=await db().from('applications').insert({name,enrollment,email,details}).select('id').single();if(error){if(error.code==='23505')return NextResponse.json({error:'An application already exists for this enrollment number or email.'},{status:409});return NextResponse.json({error:'Applications are not available yet. Please contact the club team. Your form has not been submitted.'},{status:503})}return NextResponse.json({id:data.id},{status:201});}catch{return NextResponse.json({error:'Unable to submit this application. Please try again.'},{status:400})}}
